// Runtime config injected at deploy (empty = client-side demo mode).
// Set SUPABASE_URL + SUPABASE_ANON_KEY here (or via the Pages deploy workflow)
// to route runs through the serverless Supabase Edge Function.
window.PRIORAUTH_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
}
